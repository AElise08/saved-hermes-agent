services:
  agent:
    build: .
    environment:
      AGENT_ID: ${AGENT_ID:-saved}
    env_file:
      - path: ./plow-credentials
        required: false
    volumes:
      - agent-home:/var/lib/hermes
    stop_grace_period: 35s

volumes:
  agent-home:
